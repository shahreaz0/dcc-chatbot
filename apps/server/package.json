{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@ai-sdk/devtools": "^1.0.1",
    "@ai-sdk/google": "^4.0.7",
    "@dcc-chatbot/db": "workspace:*",
    "@dcc-chatbot/env": "workspace:*",
    "ai": "catalog:",
    "dotenv": "catalog:",
    "evlog": "catalog:",
    "hono": "^4.12.27",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@dcc-chatbot/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.22.3",
    "typescript": "catalog:"
  }
}
